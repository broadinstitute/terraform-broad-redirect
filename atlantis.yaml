---
version: 3
projects:
  - name: production
    apply_requirements:
      - approved
      - mergeable
      - undiverged
    autoplan:
      enabled: true
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "../atlantis.yaml"
    dir: prod
